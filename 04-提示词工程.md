# 04 · 提示词工程：任务书、派发包与「复制即用」提示词

> 提示词在本体系里不是聊天话术，而是**带验收标准的契约**。
> 本篇给出三模式产物、10 要素、执行提示词 11 模块、出稿自检门禁。

---

## 1. 心智：提示词 = 可验证的契约

普通提示词问「帮我做 X」；契约式提示词规定：
**X 做到什么程度算完成、允许碰什么、不许碰什么、拿什么证明、卡住了怎么报告。**

由此推出三条生成纪律：

1. 目标写成**可验证的成功标准**（可用 GIVEN/WHEN/THEN 句式），不是动作描述；
2. 没决定的关键问题不得写进契约（「伪装成既定事实」是返工的头号来源）；
3. 每次只输出**一个**完整提示词——多任务多提示词 = 范围失控。

## 2. 三模式产物

同一套要素标准，按**受众与信任边界**分三种产物（任务书生成器的设计思路即建立在此三分上）：

| 模式 | 受众 | 用途 | 特征 |
|------|------|------|------|
| **模式一 · 内部任务书** | 协调者自己留痕 → 派发内部执行 Agent | 拆包、授权、验收落盘 | 含状态头块、步骤表、任务包字段 |
| **模式二 · 跨应用派发包** | 另一款工具里的 Agent，由人类复制转发 | 跨信任边界派发 | 头部加「转发指令」，注明本端收发纪律不适用、交付最高「待复核」 |
| **模式三 · 外部站点提示词** | 人类拿到图像生成/搜索等站点直接用 | 不消耗 token 的外包路径 | 「复制即用」正文按目标站点 schema 组织 + 变量说明 + 迭代记录预留 |

触发判据也写成一句话：**这份产物是否「需要人类复制拿走」？**
自用留痕 → 一；转给其他应用 → 二；拿出去给站点/人 → 三。判不清就追问一句，别猜。

**模式三的「复制即用」工作流**四步：
① 问清目标与站点 schema → ② 按骨架组装（如图像类六块：主体任务/构图布局/风格材质/
文字标签（需一字不差的写死并加引号）/比例格式/约束负面）→
③ 标出可替换变量与当前取值 → ④ 预贴 v1、留「迭代记录」栏让人类带结果回来迭代。

## 3. 10 要素表（模式一/二逐项必查）

| # | 要素 | 要点 | 口述缺失时 |
|---|------|------|------------|
| 1 | 目标 | 可验证成功标准（GIVEN/WHEN/THEN 可选） | 追问「做到什么程度算完成」 |
| 2 | 范围（文件集合） | 做什么/不做什么；授权修改的文件清单 | 追问边界 |
| 3 | 授权包络 | L0/L1 + C 级判定，写明当前处于第几次决定 | 默认 L1，判不清追问 |
| 4 | 步骤与逐步验收 | `- [ ] T001 动作（精确路径）→ 验收：判据`；并行标 `[P]`，依赖内联 `(depends on T0xx)` | 生成方拆解，交人确认 |
| 5 | 数据等级 | D1 默认 / D2 需逐任务授权 / D3 拒收 | 默认 D1，不追问 |
| 6 | 禁止项 | 明确红线（含「不碰并行任务文件」类声明） | 追问「有没有红线」 |
| 7 | skills 字段 | 值来自路由表；无合适项则显式 `[]` + 理由，**缺理由不得出稿** | 查路由表 |
| 8 | 交付类型 | ack / progress / blocked / result 之一 | 默认 result |
| 9 | RUN/证据路径 | 绝对路径，并注明执行者须登记**身份 + 真实模型名** | 按目录约定生成 |
| 10 | 回执要求 | 只回「结论 + 指针 + 简短摘要」，不回全文 | 套默认回执句 |

判级辅助（轻量三问，对应 C1–C4）：改动面是否跨多文件？是否有不可逆/生产操作？
是否需人类中途拍板？——三问皆「是」倾向 C4，皆「否」倾向 C1–C3。

## 4. 执行提示词的 11 个强制模块

派发给执行 Agent 的正式提示词（模式一/二的正文）按 11 模块组织——
这份清单是早期实践中固化的执行提示词结构，是「设计先于执行」在文本上的形状：

| 模块 | 必须包含 |
|------|----------|
| 1. 称呼与输出纪律 | 固定抬头规则（如每次回答仅在开头一次称谓），抑制噪声输出 |
| 2. 项目背景 | 版本、架构、环境、已完成任务、已知故障、**当前事实**（区分事实与猜测） |
| 3. 本轮目标 | 必须完成的结果，而非动作描述 |
| 4. 范围与禁止项 | 允许修改哪些模块；哪些问题只登记不修 |
| 5. 调查要求 | 先追调用链、数据流、依赖和真实根因，不边查边改 |
| 6. 实施约束 | 最小修改、兼容策略、错误处理、安全与隐私要求 |
| 7. 测试要求 | 新增测试、现有全量测试、接口契约、异常路径 |
| 8. 真实验证 | 健康检查、真机/生产冒烟、不能自动完成的待验收项 |
| 9. 构建与发布 | 版本号、产物、SHA-256、下载复核、回滚基线 |
| 10. 防卡死规则 | 超时、实时输出、失败重试上限、禁止重复盲跑 |
| 11. 输出格式 | 证据、风险、未验证项、下一步；**唯一完成标记**（如「#15 任务完成」） |

三个容易被忽略的细节：

- **唯一完成标记**：让回收方机器可判「做没做完」，不靠读散文猜；
- **防卡死**：失败命令最多原样重试一次；连续数分钟无输出先查进程/日志/锁，
  不得直接重复启动；命令保留实时输出，不得用过滤管道隐藏长期任务全过程；
- **P0 安全例外**：发现提示词之外的 P0 安全问题可以止血，但必须报告原因和最小范围
  ——契约允许最窄的例外，而不是死板或双标。

## 5. 执行端三件套（贴在任务书尾部）

每个任务书尾部固定附三样东西，预防执行端最常见的三种失败模式：

1. **Before You Begin**：有疑问先问再动手，**一次 ≤2 问**（防连珠炮审讯）；
   问完即执行，不反复确认（防把追问当免责仪式）。
2. **升级条件**：出现下列情形必须按 02 篇三要素格式 `blocked` 上报——
   任务要素与本端职责/授权不符；需越出授权文件清单才能完成；授权前提失效；
   同一失败命令原样重试一次后仍失败且无法归因。
3. **回执样例**：
   `result｜<结论一句话>｜指针：<产物/报告路径>｜三句话｜真实模型：<运行时实际模型名>`

回执样例值得单独强调：**要求登记真实模型名**，因为「谁干的」在模型路由
可能被静默改变的环境里，是复核可信度的前提（换模型 = 换能力基线）。

## 6. 出稿自检门禁（生成方义务）

任务书写完不算完，逐项打勾才允许交付：

```
□ 10 要素齐全（模式三对照目标站点 schema 核对结构块无缺）
□ 元信息头块完整（状态/日期/派发者/执行者/授权）
□ skills 字段非空值在路由表存在；为空则显式 [] + 理由——缺理由不得出稿
□ 每步骤有验收判据；[P]/(depends on …) 与禁止项中的并行声明一致
□ 数据等级已定（D1 默认 / D2 已获授权 / D3 已拒收）
□ RUN/证据路径为绝对路径，且含「登记身份 + 真实模型名」义务行
□ 交付类型 ∈ {ack, progress, blocked, result}
□ 回执要求写明「只回结论 + 指针 + 简短摘要，不回全文」
□ 执行端三件套已附尾部
```

**精简档**：C1 小任务只保留元信息头、目标、步骤与验收、任务包字段、回执要求五节，
禁止项与三件套各留一行——防止为小任务写三页契约（形式主义也是范围蔓延）。

## 7. 任务拆分配套规则（生成任务书时的裁剪判断）

- 一个任务只解决**一个主问题**，但必须验证其相邻影响；
  主任务编号 `#01…`，插单排障 `#EX_01…` 两套编号独立；
- 理想粒度：一次连续执行内能完成「调查 → 最小修改 → 测试 → 交付」；
  跨子系统则拆成多任务或多可回滚批次；
- **调查任务默认只读**；清理、迁移、删除必须另开任务并等待明确授权；
- 重要任务先建立基线再修改，修改后对照基线复核；
- 禁止「全面重构 / 修复所有问题 / 补齐所有测试」式无边界大任务。

## 8. 迷你示例：同一需求的坏契约 vs 好契约

> 坏：「帮我优化一下检索模块，顺便把测试补一补。」
>
> 好（节选）：
> ```
> 目标：给定 20 条抽样查询，top-5 命中率从 57.9% 提升至 ≥70%（脚本 verify 复跑取证）
> 范围：只改 db 索引层（description/tags/检索文本拼接）；
>       不做：不改 skill 真源、不改表结构、不跑全量
> 步骤：T001 只读复现基线 → 验收：报告含逐样本 HIT/MISS 表
>       T002 手段 A + 重嵌入 → 验收：同口径复跑，数字与构成变化并列
> 数据等级：D1；skills: []（理由：数据工程运维，路由表无对应域）
> 禁止项：不得为提分修改评估脚本；不动并行任务台账文件
> 回执：result｜结论｜指针：RUN 报告路径｜三句话｜真实模型：<运行时登记>
> ```

差别不在字数，在于**执行端能否独立回答「做没做完、能做多宽、卡住怎么办」**。

## 9. 需求侧的对应纪律

任务书只是执行侧契约；上游还有需求侧六条（同样出自早期沉淀的章程条款）：

1. 先说「为什么做」，再说「做什么」，最后才讨论「怎么做」；
2. 每个需求写出主流程、异常流程、边界条件、权限与数据影响；
3. 必须明确「不在本轮处理」的内容（防执行模型顺手扩张）；
4. 架构类先给 2–3 个可行方案 + 代价 + 推荐，由人选择；
5. 需求确认前不进实现；人明确说「先讨论」时不得提前执行；
6. 新项目首次会话先填启动模板（目标/范围/环境/数据与权限/现有资产/
   质量目标/约束/验收），未明确项先讨论，不直接开工。

## 10. 本篇小结

- 提示词工程的可控性来自**结构强制**：10 要素管「写没写全」，
  11 模块管「执行契约完不完整」，门禁清单管「敢不敢出手」；
- 三模式区分的是**受众与信任边界**，不是格式偏好；
- 追问 ≤2 问、一次一契约、唯一完成标记——每条都在省人类的注意力、
  省 Agent 的上下文，两头都省。

上一篇 ← [03-skill治理](03-skill治理.md) ｜ 下一篇 → [05-工程纪律](05-工程纪律.md)
