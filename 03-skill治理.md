# 03 · Skill 治理：把方法论沉淀成可路由、可审计的资产

> 3.0 → 3.1 的核心增量。回答：AI 会用的方法论（skill）如何避免
> 「装了没人用、用了没挂对、挂了是坏的」三连。

---

## 1. 问题：为什么会写字段的框架仍然静默失败

给 Agent 配了几十个方法论 skill 之后的典型烂状（全部真实出现过）：

- 任务明明属于某 skill 的领域，Agent 却**裸跑**——skill 形不存在；
- 路由表声称「覆盖了所有 skill」，实测**核心 66 个、表里只引用 54 个**，12 个漏收
  （自述「skill 名与核心目录实际一致」本身就是不实的）；
- 两端目录各存一份物理副本，改了一端另一端不知道（副本漂移）；
- 台账/报告照抄外部仓库自述，装上才发现货不对板。

结论：**有资产不等于有治理**。3.1 用五个机制补齐：挂载字段、动手前四查、
路由表单源、质量盘点、命中率优化。

## 2. 挂载机制：skills 字段 + 动手前四查

### 2.1 skills 字段（任务包必填）

任务包定义必须含 `skills` 字段，与「数据等级」「禁止项」并列：

| 写法 | 含义 |
|------|------|
| `skills: [a, b]` | 值来自路由表默认项，或协调者显式覆盖 |
| `skills: []` | 显式声明本任务无需 skill，**必须同行注明理由** |
| 缺省该字段 | 拆包不合格，执行端可据此直接 `BLOCKED` |

「显式空 + 理由」是关键设计：逼着拆包方**有意识地**回答「这个任务域有没有对应方法」，
把「忘了挂」和「确实不需要」区分开——前者是漏洞，后者是信息。

### 2.2 动手前四查（执行端义务）

执行端接手任务包后，动手前依次：

1. **查授权**：核对包络（范围/数据等级/禁止项），越界即 `BLOCKED`；
2. **查 skill**：加载 `skills` 字段指定的全部 skill；
3. **查工具**：确认所需工具链/依赖可用，缺失即报告（注意：工具查不到 ≠ 没装，
   可能是当前进程环境变量过期，先从系统注册表刷新路径复验再下结论）；
4. **查缺口**：任务属已知任务域但无对应 skill，回报并附「建议补 skill X」。

上下文被压缩/会话刷新后，**重读任务文件的 `skills` 字段与授权包络**——
契约在文件里，不在记忆里。

### 2.3 漏挂即 BLOCKED，但缺口允许裸做

两条规则精确区分两种「没有 skill」：

- **漏挂**（路由表有、任务包没写、或写了没理由）→ `BLOCKED`，不得静默裸跑；
- **缺口**（该域确实无 skill，路由表标「⚠️ 缺口」）→ 照常执行但**显式记录**：
  宁裸做 + 记录，不硬套近似 skill 充数；交付中注明「未挂 skill、裸做原因、建议方向」。

「宁裸做不硬套」的理由：错误 skill 会把任务带进错误方法论，比没有方法论更糟；
而诚实的裸做记录会积累成补装依据——缺口积累到阈值 → 补装 + 更新路由表。

## 3. 路由表：高频信息的单一真源

「任务域 → 归谁 → 默认 skill」的映射放在独立的 `SKILL-ROUTING.md`，与总则正文分离：

- **为什么分离**：总则是低频变更的宪法，路由是高频变更的通讯录；
  合在一起会让每次加 skill 都要动总则（变更成本 + 膨胀风险）。
  总则里只留一行指向路由表。
- **单一维护者**：路由表只由协调者维护（改映射自便、标注日期；**装新 skill 需人批准**）。
- **表的结构**分三层——L1 元规则（这张表怎么用、选错怎么纠）→
  L2 路由矩阵（域 × 归属 × 双端 skill × 状态）→ L3 具体 skill 本体。
  缺了元规则层的平铺表，会被读的人当字典乱翻。
- **账实一致要被检验**：表里引用的 skill 名必须与核心目录实际一致——
  这句话曾经「自述为真、实测为假」，所以它现在是自检脚本的检查项而非声明。

## 4. 装 skill 门禁：把供应链信任写成清单

外部 skill 仓库是第三方代码/指令的注入渠道，安装 = 修改协作框架，按契约级流程执行。
五道闸门，任一不通过即不装：

```
0. 前提    查重：确认核心库无同域件、确属需求缺口；「拟装 + 用途 + 数据等级」入任务文件
1. 验来源  仓库 URL、作者/维护者、license 对照白名单
           （MIT/Apache-2.0/BSD-3 通过；GPL/AGPL 传染型需单独评估；无 license 不合格）
2. 验版本  不拉默认分支最新；pin 到具体 commit hash 或签名 tag，pin 值落任务文件
3. 验完整性 本地算 SHA256 与来源发布校验值比对；git 仓的等价锚点 = pin commit +
           逐件 SKILL.md 哈希，拷入后与源比对一致
4. 人工闸门 来源/版本/完整性结果 + 安装方案（目录、依赖、读写范围、回滚）提交人批准
5. 装后自检 装入单一真源 → 重建两端链接 → 跑框架自检脚本，全绿才可交付
```

实战价值案例：某官方生态的 skills 仓整树无任何授权声明（GitHub API license=null、
README/package.json/各 SKILL.md 全文 grep 零命中）——按「无 license 不合格来源」
当场 blocked，交人拍板后才以例外许可安装。**门禁的意义不是拦坏人，是让「装了什么、
凭什么装」三个月后仍可回答。**

配套细节：装前跑**冲突预检**（新名字 vs 现有核心、各端链接目录、托管项，同名即 blocked，
不得自动加前缀了事）；装入件逐字节不动，frontmatter `name` 与目录名逐件一致
（名实错配会让按名统计的自检/台账/路由全部误判）。

## 5. 单一真源 + 薄 adapter

多个 CLI 各有一个 skills 目录时，**物理副本是漂移之母**。治理结构：

```
单一真源 skills-core/（一份中立 Markdown，唯一可编辑处）
   ├─ 端 A adapter 目录 → 逐项 junction 指向核心
   └─ 端 B adapter 目录 → 同上
```

- 文件系统不再表达归属：全量挂载、「谁能看见」一样；「谁该用」归路由表管。
- 副本比对要**比内容不比时间戳**（复制文件会刷新 mtime，时间戳假漂移是噪声；
  反向，mtime 相同也不保证内容相同）。
- 台账只比对主文件哈希还不够——aux 引用文件（references/、脚本）是否同步要单列检查；
  上游整包的件与上游版本是否同步，是另一类「源侧未知项」，应显式登记。

## 6. 质量盘点：五信号 × 四档分层

定期（或按需）对全量 skill 做体检，防止库变成只进不出的仓库。**方法先定后套**，
保证可复核：

### 五信号

| 信号 | 内容 |
|------|------|
| ① 结构质量 | frontmatter 完整、description 含触发词与不适用边界、正文有可执行步骤；对照失效模式：sprawl（过长稀释注意力）、sediment（写死数字/清单随时间腐化）、dangling（引用不存在的件） |
| ② 使用率 | 按历史任务台账**脚本实测**反查引用数，不用估计 |
| ③ 重叠簇 | description 语义重叠分组，判主次 |
| ④ 漂移风险 | 与全部物理副本 SHA256 比对 |
| ⑤ 可替换方向 | 只标方向不做外部调研（调研是另一任务） |

### 四档

| 档 | 判据 | 处置 |
|----|------|------|
| 扎实 | ≥1 引用 且结构无硬伤 | 保留 |
| 可用但有瑕疵 | 有引用/有域价值，但有可指认具体缺陷 | 逐条列缺陷，限期修 |
| 存疑 | 0 引用 且质量或适配性弱 | 保留观察或待停用 |
| 建议合并或替换 | 处于明确重叠簇、有具体处置方向 | 交人拍板，不代表内容差 |

盘点纪律：全程**只读**；所有「合并/替换/停用」是建议，留人逐条拍板；
每个结论附实测证据（数字、路径、哈希），禁止「整体感觉不错」。
一次真实盘点的产出形态：67 件 → 扎实 25 / 瑕疵 14 / 存疑 23 / 合并替换 5，
并顺手发现两处**账实错配**（注册名与目录名不一致 → 按名统计全线误判）。

## 7. 命中率优化实录：57.9% → 78.9%

有了 skill 库和「任务→skill」向量路由后，下一个问题是：**语义检索真的选得对吗？**

### 背景

66 个 skill 入库建向量索引（本地 embedding，全程离线），从 98 条真实历史任务中
抽样 20 条测 top-5 命中率。阶段 3 基线：**57.9%（11/19 可评样本）**。

### 三个手段（全部只动 db 索引层，不动 skill 真源）

1. **英文 description 补中文**（34 个件）：中文任务 query 与英文描述向量距离远；
   补写中文描述（保留专有名词）。
2. **检索文本拼接 category**（66 个全覆盖）：入库文本从 `name+description+tags`
   改为再加任务域分类，强化「任务类型语义」。
3. **索引扩展：注入真实触发词**（37 个件）：前两项做完命中率**纹丝不动 57.9%**
   （构成有变、净持平）——根因是「**内容语义 > 任务类型语义**」：任务型 skill 的
   描述缺乏实际任务 query 的词汇，被内容域 skill 抢位。对策：从 78 条
   **非抽样**任务描述中提取触发词，受控截断（每条 40 字、每 skill ≤5 条）注入
   description 重嵌入。**训练/测试严格分离**防数据泄漏：扩展只用 78 条，
   评估仍用 20 条抽样。

结果：**78.9%（15/19，+21.0pt）**，11 条基线命中全保留，新增 4 条。

### 剩下 4 条为什么不救

诚实归因比刷分重要：2 条属**台账关联错位**（任务被人工关联到了语义漂移的 skill，
索引修不了标注错误）；2 条属**任务类型词汇库内无覆盖**（该类型确实缺 skill）。
各自归属：前者回台账层修正，后者进补装流程。

### 可迁移的三条经验

- 命中率异常的默认假设应该是**数据/描述问题**，不是「换更强模型」；
- 分步透明记录（每步数字 + 构成变化）才能暴露「手段 1+2 无效、手段 3 才是主力」；
  只看总分会误配药；
- 索引扩展有泛化边界：对与历史任务同主题的新任务有效，全新任务类型仍靠描述本身——
  所以未命中样本的价值恰恰是指向缺口。

## 8. 框架自检：绿灯之后还要问查了什么

以上机制需要一个可执行的看门狗：一个自检脚本，每次改总则/路由/skill 后跑。
检查面（从 3 项被踩坑后扩到 7 项）：路由表每行可解析且引用存在、skill 名与目录
账实一致、总则管理块 BEGIN/END 配对、任务包 `skills` 字段必填、两端链接指向一致等。

**血的教训**：该脚本曾经「3 项全过」绿灯运行，而底下是 12 个 skill 漏收、
副本漂移成风——**绿灯 ≠ 健康，取决于你查了什么**（详见 [06-踩坑实录](06-踩坑实录.md) 案例二）。
因此自检面本身要随事故扩张，且每次扩张在台账留痕。

## 9. 本篇小结

skill 治理的完整闭环：

```
缺口登记 → 门禁安装（来源/版本/完整性/人批准）→ 单一真源 + 薄 adapter
→ 路由表单源 → 任务包 skills 字段强制挂载 → 执行端四查
→ 落盘证据 → 定期五信号盘点 → 命中率实测优化 → 自检脚本看门 → 回写路由表
```

每一环都是对「AI 资产也会腐化」这一事实的制度回应。

上一篇 ← [02-设计与执行分工](02-设计与执行分工.md) ｜ 下一篇 → [04-提示词工程](04-提示词工程.md)
