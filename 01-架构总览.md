# 01 · 架构总览：多 Agent 拓扑与授权包络

> 本篇回答：一个 AI 参与的长期工程，**权力结构**应该长什么样。
> 演进线：从三角色最小模型到 3.0 多 Agent 拓扑；核心信条贯穿始终。

---

## 1. 起点：三角色最小模型

这套体系最早的可运行形态是一个只约束三类参与者的最小模型：

| 角色 | 必须负责 | 明确禁止 |
|------|----------|----------|
| **项目所有者（人类）** | 定义真实目标、业务优先级、风险容忍度；关键真机与业务验收 | 不负责替模型猜代码细节；不因模型声称「完成」而直接发布 |
| **顾问/协调 Agent** | 澄清需求、识别歧义、设计方案、拆分任务、编写执行提示词、审阅结果、决定下一步 | 默认不直接进入代码实现；不得在信息不足时假设现状 |
| **执行 Agent** | 读取代码、调查现状、最小实现、补测试、构建、部署、给出证据 | 不得自行扩大范围；不得用「看起来正确」代替测试与运行证据 |

两条重要洞察从第一天就写进了章程：

### 1.1 「Agent ≠ 模型」

同一个聊天工具可以在不同会话里扮演不同角色——顾问会话负责设计，
执行会话负责写码，**角色由任务和授权定义，不由模型名定义**。
这条洞察是后来 3.0 能扩展到任意多 Agent 拓扑的前提：加一个角色不需要换一个模型，
只需要登记新的「会话 × 职责 × 授权」。

### 1.2 顾问戴多顶帽子的危害

实践早期发现：让顾问 Agent 直接下场改代码，或让它用「全面重构、修复所有问题、
补齐所有测试」这类**开放式指令**派活，会造成三类事故——

- **范围蔓延**：一次改动滚成无关重构，diff 失去审查边界；
- **误判需求**：信息不足时模型不会停下，而是把猜测写成既定事实继续推进；
- **执行者过载**：一个任务塞多个目标，做完没有任何一项可单独验收。

对策就是把「帽子」拆成显式角色，并用制度禁止越界——这就是 3.0 的由来。

## 2. 3.0 多 Agent 拓扑

当执行侧出现了纯文本、图像识别、多模态生成等不同能力的模型，拓扑从三角形长成树形：

```text
项目所有者：最终决策与人工验收（唯一）
└─ 协调者 Agent（Coordinator）
   ├─ 文本执行 Agent：非多模态执行与编程
   ├─ 识别 Agent：图片识别/理解 + 对生成结果的读图审核
   └─ 多模态执行 Agent：图片修改/生成、复杂多模态执行与编程
      └─ 独立复核 Agent（新窗口）：复杂交付的第三方复核
```

设计要点：

1. **所有者只有一个**，且拥有最终验收权；任何 Agent 不得代替人类宣布 DONE。
2. **协调者是状态的唯一写入者**（见 [02 §6](02-设计与执行分工.md)），负责拆包、路由、回收、复核。
3. **执行端按能力分档**，而不是按「谁的模型更强」排座次；路由判定只看一个问题：
   *任务要正确完成，是否必须直接理解或处理非纯文本信息？*
4. 复核者必须是**独立会话/独立窗口**——执行者不得成为自己工作的唯一审查者。
5. 跨应用边界的 Agent（如另一款 CLI 里的模型）由人类手动转发派发，
   协调者**不得自动调用**——自动化止步于信任边界。

### 角色随规模伸缩

- 单人单工具：所有者 + 协调者可以是同一个人的两个会话；
- 只有文本活：识别档与多模态档合并进执行档，拓扑退化为最初的最小三角形；
- 关键判据**不变**：写入权单一、复核独立、验收归人。

## 3. 授权包络：L0 / L1 与 C1–C4 分级

3.0 最重要的新增制度是**授权包络（authorization envelope）**：
每个任务在开工前就冻结「能做什么、做到什么程度、人要在哪几个点出现」。

### 3.1 两级模式

| 模式 | 含义 | 适用 |
|------|------|------|
| **L0 观察模式** | 每步执行前都要人类确认（四道门禁），协调者只出方案不动手 | 高风险操作、信任建立期、规则出问题时降级 |
| **L1 包络模式** | 人类只在预先约定的少数决策点出现，包络内 Agent 自动推进 | 新项目默认 |

L0/L1 不是效率与安全的取舍，而是**把安全从「每步都问」改写成「边界先定死」**：
越界即停（`BLOCKED`），边界内才自由。

### 3.2 复杂度分级 C1–C4

| 级别 | 典型形态 | L1 下人类介入次数 |
|------|----------|-------------------|
| C1 | 单文件小修、机械操作 | 2（启动批准 + 最终验收） |
| C2 | 多文件、有测试可验证 | 2 |
| C3 | 跨模块新功能 | 2 |
| C4 | 生产操作 / 批量删除迁移 / 不可逆风险 | 3（契约批准 + 精确执行批准 + 最终验收） |

**两次决定设计**（C1–C3）：

1. **启动批准**——所有者批准授权包络（目标、文件集合、数据等级、禁止项）；
2. **最终验收**——所有者对着证据做人工验收，此前任何 Agent 不得写 DONE。

中间的执行、回收、技术复核全部由协调者在包络内自动完成，**不设独立结果回收批准**——
这是 L1 与 L0 的本质区别：人审批的是「边界」，不是「每一步」。

**C4 的三次决定**多出一道「精确执行批准」：Agent 先交冻结清单（逐文件、逐步骤、
回滚方案），所有者对这份清单点名批准后才准动手。理由很直白——不可逆操作出错的
代价不对称，值得多花一次人类注意力。

### 3.3 重新批准触发器

包络一旦被突破，自动推进立刻失效，任务回到「等待人类决定」状态。触发条件（任一）：

- 项目根目录、目标、文件集合、读写范围发生变化；
- Agent / 模型 / 渠道 / 数据等级发生变化；
- 操作类型变化（从只读变成写入、从写入变成删除/迁移）；
- 回滚能力丧失或不明；
- 出现结果不一致、越界写入、安全异常、疑似数据损失。

纪律：**不得自行扩大范围来「顺手」突破包络**——变化了就去申请重新批准，
这比先斩后奏再解释便宜得多。发生状态漂移或越界执行时，当前任务降回 L0，
恢复 L1 需所有者决定。

## 4. 五条铁律的制度化

三条核心信条（需求与设计先行 / 执行与验证分离 / 以证据判定完成）
在 3.0 中展开为五条铁律，每条都有对应制度承接，不靠自觉：

| 铁律 | 落地制度 | 详见 |
|------|----------|------|
| 显性需求优先于猜测 | 任务书 10 要素 + 缺失字段追问（一次 ≤2 问）；「没有决定的关键问题不得伪装成既定事实」 | 04 |
| 设计先于执行 | 角色职责禁令（顾问不下场、执行者不决策）+「先讨论」状态机：需求确认前不进实现 | 02 |
| 范围锁定 | 授权包络 + 禁止项清单 +「明确不做的事情」必写 + 任务外问题只登记不修 | 02 / 04 |
| 证据优于自述 | 状态词汇表（无证据不得 PASS）+ RUN 报告落盘 + 独立复核门禁 | 05 |
| 可回滚 | 重要任务先建基线 + 发布产物记哈希 + 回滚方案写明代码/数据/配置三层 | 05 |

## 5. 规则自身的演进纪律

这份架构不是设计出来的，是**踩出来的**——总则自身的维护方式也是一条规则：

1. **只新增，不改写**已冻结条文（3.0 → 3.1 的挂载机制就是作为 §13 增量追加的）；
2. 新增规则必须解决**真实重复发生**的问题，防止条文无限膨胀；
3. 项目特有规则写进项目仓库，通用层只留方法论；
4. 高频变更的路由信息（谁能干什么）与低频变更的总则正文**分文件维护**；
5. 总则与项目明确需求冲突时，先指出冲突，由所有者决策——Agent 无权静默择一。

## 6. 反面模式速查（架构层常见病）

| 反面模式 | 症状 | 对应解药 |
|----------|------|----------|
| 全能会话 | 一个 Agent 既设计又实现又验收，自说自话 | 角色拆分 + 职责禁令（§1.2） |
| 双真源 | 状态同时存在两处「都算数」的记录 | 单一写入者（02 §5） |
| 静默升级 | 执行中发现范围不对，自行扩大继续跑 | 重新批准触发器（§3.3） |
| 假复核 | 复核者读过执行过程、或就是执行者本人 | 独立窗口、只看产物与判据（§2 要点 4） |
| 一步一审批 | 每行改动都问人，人类成为瓶颈后开始「盲签」 | L1 包络模式：批边界不批步骤（§3.1） |
| 模型名当能力 | 以为换旗舰模型就不用分工 | Agent≠模型 + 能力分档路由（§1.1、§2） |

「一步一审批」值得多说一句：它看似最安全，实则最危险——人类疲劳后的习惯性
「同意」会把审批制度掏空成橡皮图章。**保护人类注意力的方式是让它出现在
数量少、信息全的决策点上**，这就是两次决定/三次决定设计的行为学依据。

## 7. 本篇小结

- 多 Agent 架构的本质不是「多用几个模型」，而是**把信任写成结构**：
  单一所有者、单一状态写入者、独立复核、边界内自动。
- 授权包络让人类注意力花在两个最值钱的点上：**定边界**与**终验收**。
- 铁律必须挂制度，没有落地机制的铁律只是口号。
- 规则要允许自己长大：增量修订、防膨胀、通用与项目分层。

下一篇 → [02-设计与执行分工](02-设计与执行分工.md)：这套拓扑在单个任务粒度上怎么跑。
