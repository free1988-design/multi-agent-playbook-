# 02 · 设计与执行分工：单个任务怎么在多 Agent 间流动

> 01 篇讲权力结构，本篇讲**任务粒度上的操作规程**：
> 谁判断、谁动手、状态怎么流转、证据怎么回收、多模态怎么路由。

---

## 1. 铁分工：设计只判断，执行只执行

| 端 | 只做 | 禁做 |
|----|------|------|
| **设计/协调端** | 澄清需求、识别歧义、给方案取舍、拆任务、写执行提示词、回收复核、决定下一步 | 直接改代码（默认）；信息不足时假设现状；声称自己看过没看过的材料 |
| **执行端** | 读现状、追根因、最小实现、补测试、构建、按格式交证据 | 自行扩大范围；改任务状态文件；用「看起来正确」代替证据 |

两侧各有一条容易被忽视的禁令，都是事故换来的：

- 协调端**不得静默把设计工作降级甩给执行端**——执行端拿到含糊任务包时有权 `BLOCKED` 退回；
- 执行端**发现任务外问题只登记、不顺手修**——修 = 制造 diff 之外的暗改动，复核失效。

## 2. 任务状态机

### 2.1 状态词汇

任务生命周期（协调者维护，单一写入）：

```
DISPATCHED → IN_PROGRESS → DELIVERED(=result 待回收)
                    ↘ BLOCKED →（重新授权后）IN_PROGRESS
DELIVERED → REVIEW_PASS → ACCEPTED（人类验收）→ DONE
         → REVIEW_FAIL(附差距清单) → REWORK → DELIVERED（循环直至通过）
                                  → REDESIGN（推翻方案，回协调端）
任意态 → CANCELLED / FAILED
```

执行端交付类型只有四种：`ack`（确认收到）/ `progress`（进度）/ `blocked` / `result`。
执行端可交付的最高状态是「待复核」（PENDING_REVIEW），**永远不是 DONE**——
DONE 只能由协调者在人类验收后写入。

### 2.2 失败与返工

- **REWORK 必须附差距清单**：复核不通过不许只说「不行」，要逐条写
  「预期 / 实际 / 位置」，执行端按清单返工，不猜意图。
- **REDESIGN 是升级不是失败**：当返工循环两轮仍不收敛，说明错在方案不在实现，
  退回设计端重出契约——继续让执行端试错只会烧掉上下文。
- **发现冲突即返工**：多个并行任务的产物互相冲突时，不是「谁后写谁赢」，
  而是全部标记冲突文件、按交接包契约重做受影响步骤。

### 2.3 BLOCKED 三要素（缺一项按不合格退回）

```
1. 卡在哪：具体步骤、具体命令/文件、报错原文（可含行号）；
2. 已试什么：每次尝试 + 结果，含「同一失败命令已原样重试一次仍失败」的声明；
3. 需要什么帮助：要授权 / 要信息 / 要环境 / 要人决策，四选一并写明。
```

`BLOCKED` 是**合法交付**，不是绩效污点。制度目标恰恰是让它便宜：
报告一条坏消息的成本必须远低于擅自闯一条红灯的成本。
常见触发情形：任务要素与本端职责/授权不符；需越出授权文件清单才能完成；
授权前提失效；同一失败命令重试一次仍无法归因。

### 2.4 回收三步

协调者回收 `result` 时：① 对照任务书逐项核验收判据；② 核 diff/证据与声明一致；
③ 更新状态并落 HISTORY。**「执行完成」≠「通过」**——通过与否由证据判定。

## 3. 多模态两档路由

路由判定只看一个「唯一判定问题」：*任务要正确完成，是否必须直接理解或处理
图片像素、视频画面、音频、扫描件、视觉布局等非纯文本信息？*

| 判定 | 归属 | 例 |
|------|------|----|
| 否 | 文本执行端 | 源码、结构化数据、文件元数据、命令行逻辑、纯文本处理 |
| 是，且属**识别/理解**（只读不产出） | 识别档 | 描述截图内容、OCR 扫描页、分析图表、理解 UI 布局、审核生成图是否达标 |
| 是，且属**修改/生成或复杂多模态** | 多模态档 | 改图生图、视频画面、音频内容、视觉布局调整、音视频处理代码 |

三条要点：

1. **写代码不改变路由**——「给视频做检测代码」仍归多模态档，路由看的是
   正确完成所需的能力，不是产出的形态。
2. **识别档给生成档当眼睛**：多模态 Agent 产出的图片，由识别 Agent 读图审核
   （生成者自评不可信——它看得见自己想看见的东西）；缺这步审核，结果只能标 `UNVERIFIED`。
3. 判不清归谁 → `BLOCKED` 退回协调端拆包，执行端**不得自选档位**。

## 4. 混合任务强制拆分

一个需求同时含多模态与非多模态部分时，协调端**必须拆成至少两个任务包**：

- 文本包：逻辑、数据、普通代码、机械处理；
- 识别包（如需要）：读图出结论，交付结构化描述；
- 多模态包：生成/修改本体 + 直接相关代码。

每个包写明**输入、输出、依赖、数据等级、禁止项、交付路径、复核者**，
依赖显式成文（`depends on T0xx`）或走接口文件交接。协调端负责依赖排序与接口对账；
识别/多模态 Agent 不得声称文本包已验收，反之亦然。

## 5. 交接包：跨会话/跨 Agent 的冻结契约

任务包被称作「交接包」的原因：换会话、换模型、上下文被压缩，
唯一可靠的是**落盘的契约**。规则三条：

1. **契约冻结**：文件清单 + 步骤验收 + 范围 + 「禁止格式化追改」四件套写进任务书；
   执行端只能改授权清单内的文件，**禁止顺手改格式**（格式化噪声会淹没真实 diff，
   让复核退化为重新通读）。
2. **状态单一真源**：项目状态、任务状态、历史归档文件只有协调端可写；
   执行端只写自己的 RUN、证据与发件箱指针。两个真源 = 没有真源。
3. **回执不回全文**：执行端回传 =「结论 + 指针 + 三句话」，全文躺在证据文件里。
   这是协调端上下文保护的关键——详见 [05 §6](05-工程纪律.md)。

## 6. 执行验证分离：六步标准流程

长期实践沉淀、3.0 沿用的单任务闭环：

```
1. 执行端先复述问题（暴露理解偏差，成本≈0）
2. 协调端指出歧义，列出未决问题
3. 给 2–3 个方案 + 代价 + 推荐意见（不替所有者拍板）
4. 等所有者选择（架构/范围/风险决策必须归人）
5. 确认后输出【唯一】执行提示词（多任务多提示词=范围失控）
6. 执行端交付后，由【独立】复核判闭环，缺口下发修正任务
```

「独立复核」的最小定义：**没有参与执行、没有看过执行过程的思考链、
只看产物与判据**。同一会话自己写自己审，不算复核。

以下情形强制追加一次独立只读复核：

- 架构级重构、跨多子系统改动；
- 鉴权、安全、权限、加密、日志脱敏；
- 数据库迁移、批量数据修改、生产清理；
- 并发、状态机、支付或不可逆操作；
- 长任务中多次改变方案，或测试为了通过被大幅重写；
- 所有者无法亲自做完整真实验收的交付。

## 7. 发布原则

- 「全量单元测试通过」不是发布充分条件：核心真机冒烟、已知 FAIL 审批、
  UNVERIFIED 清单、回滚产物**四者齐备**才算可发布。
- 发布产物记录版本号、大小、SHA-256；分发端下载后再次校验哈希。
- 回滚不是一句 `git revert`：代码、配置、数据库、客户端各自怎么退，写清楚才算。

## 8. 本篇小结

- 分工的本质是**动机分离**：设计者不承担「做完了吗」的压力，执行者不承担
  「该不该做」的权力，复核者不承担与前两者同步人情的义务。
- 状态机 + 单一写入者 + BLOCKED 三要素，让「卡住」成为可管理状态而非事故。
- 多模态路由、混合拆分、交接包、独立复核——全部服务于同一目标：
  **让每一次 Agent 输出的可信度都有明确的证据来源**。

上一篇 ← [01-架构总览](01-架构总览.md) ｜ 下一篇 → [03-skill治理](03-skill治理.md)
