# 05 · 工程纪律：状态词汇、四实践原则、TDD 与复核门禁

> 02 篇管「谁做什么」，本篇管「做成什么样才算数」。
> 三条核心信条的工程条款 + 四实践原则 + 上下文保护，合并执行。

---

## 1. 三条核心信条的工程化落点

「需求与设计先行、执行与验证分离、以证据而不是模型自述判定完成」
落到日常工程，是三组具体机制：

- 需求设计先行 → 六步流程与「先讨论」禁令（见 [02 §6](02-设计与执行分工.md)）；
- 执行验证分离 → 独立复核 + 自动化测试/CI 不许「只为通过而降断言」；
- 证据判定 → 本节的状态词汇 + RUN 落盘 + 发布四件套。

## 2. 状态词汇表：禁止模糊宣称完成

所有技术结论只允许用这套词，**禁用「应该没问题 / 基本完成 / 看起来 OK」**：

| 状态 | 含义 | 使用义务 |
|------|------|----------|
| `PASS` | 有代码证据，并通过相应自动测试或真实验证 | 报告须附可复跑证据（命令 + 输出摘要） |
| `FAIL` | 已确认损坏或验收未通过 | 附操作步骤、预期、实际、版本、时间、日志编号 |
| `RISK` | 高概率风险，尚未复现或证据不完整 | 写明风险面与建议缓解，不得悄悄消化 |
| `UNVERIFIED` | 缺真机/生产/必要外部条件验证 | **无法真实验证时必须标此，不得声称已通过** |
| `DEPRECATED` | 已明确下线 | 不应再有用户入口或运行时触发 |
| `LEGACY` | 仍为兼容保留 | 必须标注依赖、流量与退役计划 |

配套两条：

- **测试不得只验证实现细节**，更不得为通过而降低断言——「测试为了通过被大幅重写」
  本身是触发独立复核的信号（02 篇 §6）；
- 全量测试通过 ≠ 发布就绪：核心真机冒烟、已知 FAIL 审批、UNVERIFIED 清单、
  回滚产物四件套齐了才算（02 篇 §7）。

## 3. 失败升级与重试纪律

执行端对失败的标准动作，防止「越慌越跑偏」：

1. 失败命令**最多原样重试一次**；再次失败必须分析原因或更换策略，不得第三次盲跑；
2. 无法归因 → 按 BLOCKED 三要素上报（卡在哪 / 已试什么 / 需要什么），
   格式固定，不许「再试试」拖着；
3. 调查生产环境默认只读；删除、迁移、轮换、重启、清理必须**单独授权**，
   破坏性操作前必须有备份、影响范围、验证命令、回滚方案四件；
4. 报告与日志中**禁止出现**完整 Token、密码、密钥、设备唯一标识、敏感正文
   ——这是硬门禁不是建议（通用化处理见 06 篇与 README 边界声明）；
5. 环境异常先验现实再下结论：命令找不到 ≠ 未安装，可能只是进程环境变量过期，
   从系统注册表刷新后复验（真实误判教训，见 06 篇附注）。

## 4. 四实践原则（针对 LLM 编码弱点的行为约束）

来自对 AI 编码失误模式的观察，四条全部写成可检查的行为：

### 4.1 编码前思考（Think Before Coding）

- 不假设。有歧义时**先摊开多种解释请人选择**，不静默挑一个；
- 有更简单方案就直说，该顶回去时顶回去；
- 动手前显式声明假设：「我按 X 理解，若 Y 则结果不同」。

### 4.2 简单优先（Simplicity First）

- 不做要求之外的功能、不为一次性代码造抽象、不加没被要求的「灵活性/可配置性」、
  不为不可能的场景写防御分支；
- 自检问句：「资深工程师会不会说这过度设计？」会 → 重写；
- 200 行能压成 50 行的，就压成 50 行。

### 4.3 外科手术式修改（Surgical Changes）

- 只碰必须碰的：不「顺手改进」相邻代码/注释/格式；不重构没坏的东西；
  风格随现状（哪怕你不喜欢）；
- 发现任务外问题：**记录，不删**（包括预存在死代码）；
- 自己的改动造成的孤儿（import/变量/函数）要清掉，别人的不清；
- 修改前后 diff 检查：**不得夹带**无关格式化、依赖升级、任务外重构；
  每行 diff 应能直接追溯到任务要求——这是执行端复核的第一判据。

### 4.4 目标驱动执行（Goal-Driven Execution）

- 把模糊指令翻译成可验证目标再开工：
  「加验证」→「为非法输入写测试，然后让它通过」；
  「修 bug」→「先写复现测试（红），再修到绿」；
  「重构 X」→「前后全量测试都绿」；
- 多步任务先列「步骤 → 验证」计划，每步都有判据，允许人中途纠偏。

## 5. TDD 与测试金字塔

**Red — Green — Refactor 是编程任务的默认工作流**，RUN 报告要保留三段证据：
失败测试（红）→ 最小实现（绿）→ 定向测试与回归。

测试分层最低要求：

| 层 | 验证对象 |
|----|----------|
| 单元 | 纯逻辑、错误映射、状态机、序列化、边界条件 |
| 契约 | 请求路径、Header、Body、响应结构、状态码 |
| 集成 | 数据库、服务路由、鉴权、文件与关键依赖协作 |
| UI | 关键入口、状态切换、已下线功能确无入口、回归行为 |
| 真实冒烟 | 登录、核心链路、权限、网络、更新、覆盖安装与回滚 |
| 静态门禁 | 旧接口扫描、敏感信息扫描、依赖与配置一致性 |

先跑最小相关测试再决定是否全量；发布前必须全量。
「46 套测试全绿」也不证明「卡死」这个具体缺陷已修复——**回归测试要对着缺陷写**，
总绿 ≠ 逐项绿（06 篇案例一的镜像教训）。

## 6. 上下文保护：多 Agent 时代的稀缺资源

多 Agent 协作最先耗尽的不是额度，是**每个会话的上下文**。规则：

1. **协调者不亲自读代码/写码/跑验证**，派执行端做，自己只留结论与指针；
   唯一例外：任务小到派发不划算（三问：有判断成分吗？上下文进出量大吗？下放划算吗？）；
2. **回执不回全文**（04 篇三件套之一）；证据落盘、指针回传是默认货币；
3. **契约在文件不在记忆**：上下文压缩后重读任务包（03 篇四查），
   项目规则写进仓库（PROJECT_RULES / ARCHITECTURE / API_CONTRACTS / 发布清单 /
   LEGACY / ADR / CHANGELOG），而不是只存在聊天记录——换账号、换会话不致失忆；
4. 发现信号即收缩：主会话上下文使用率过高、单会话大量读写导致显著增长时，
   主动拆有边界的步骤下放，或保存状态刷新会话；
5. Token 花在调查、测试和证据上，不花在无边界发散上；简单小修复可缩短提示词，
   但**范围、测试、回滚三段不可省略**；大问题先做只读审计，避免边查边改返工。

## 7. 防「修 A 坏 B」：变更影响表

每次变更强制回答七问（核心不是让模型更小心，而是把**相邻影响纳入验收**）：

| 检查项 | 强制问题 |
|--------|----------|
| 改动对象 | 改了哪些文件、接口、数据结构、配置或权限？ |
| 直接影响 | 目标功能的正常/异常/重试/退出/恢复路径是否验证？ |
| 相邻影响 | 共享客户端、鉴权、缓存、数据库、UI 状态或后台任务会影响谁？ |
| 兼容影响 | 旧客户端、旧数据、旧 API、覆盖安装和回滚是否仍可用？ |
| 测试影响 | 哪些旧测试必须保持通过？哪些新回归测试必须新增？ |
| 真实环境 | 哪些行为单元测试无法证明，必须真机/生产冒烟？ |
| 回滚能力 | 上一可用版本、数据备份、恢复步骤是否存在？ |

## 8. 人类验收纪律（写给人看的部分）

所有者也要守规矩，否则制度空转：

- 不只看执行端总结：核对版本号、测试数量、哈希、下载地址和**真实行为**；
- 按清单逐项验收，不因前几项成功就跳过后续；
- 发现回归记录六元组：操作步骤/预期/实际/版本/时间/日志编号；
- 重大问题优先回滚，不在生产上连续堆叠不确定补丁；
- **验收通过后明确告知**——否则后续会话仍把已完成事项当待办（状态回写对机器同样重要）。

## 9. 执行前最终检查表（十格，任务书出厂自带）

```
□ 需求与成功标准已明确？        □ 本轮范围与明确不做项写清？
□ 需要先做只读调查？            □ 列出了可能受影响的相邻功能？
□ 要求了新增测试与全量回归？    □ 明确了真机/生产验证项？
□ 设置了防卡死与失败重试规则？  □ 有版本号、产物哈希、回滚基线？
□ 禁止了敏感信息泄露？          □ 规定了唯一完成标记？
```

## 10. 本篇小结

工程纪律的共同底色：**把「完成」定义为可检查的证据集合，而不是模型的一句话**。
状态词汇、影响表、检查表都不是繁文缛节——每一条背后都有一次返工或一次误判（见 06 篇）。

上一篇 ← [04-提示词工程](04-提示词工程.md) ｜ 下一篇 → [06-踩坑实录](06-踩坑实录.md)
