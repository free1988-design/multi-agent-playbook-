# 06 · 踩坑实录：四条规则的伤疤来源

> 本体系里几乎没有一条规则是先天设计——都有对应的事故。
> 本篇按「事故 → 根因 → 固化规矩」讲五个真实案例（已全部通用化：
> 人名角色化、项目名通用化、路径与标识符抹除）。

---

## 案例一：「仓库自述不可信」——一次候选调研的失真纠错

### 事故

技能库补货流程中，调研任务对某外部热门 skill 仓库产出了候选卡片：
「专为 XX 工具写的 13 个视频制作 skill」，并据此拟定了首装清单、人类已经圈选。
下一环节（安装前的纠错复核）按目录树逐件实测后发现：

- 真实清单是 **39 个 skill 的「某作者个人工作流全集」**——视频相关仅约 14 个，
  其余是该作者的社交运营、自家平台、移动上架、博客迁移、语言教学等工作流；
- 失真的源头：调研时**照抄了仓库 README 与平台 description 字段**，
  而这两个自述本身就过期了；
- 首装清单里的四个「子 skill 名」**在仓库中根本不存在**（实际目录名是另一套带前缀的
  命名），功能件存在但名字是猜的；
- 更细的一层：拟装的配音件所用语音引擎与团队自建音色链路**并不相同**，
  「装了它就等于覆盖该能力缺口」的判断不成立；
- 还发现调研报告**内部口径自相矛盾**：以「依赖云端 API 违反离线原则」否掉一个候选，
  却同时把另一个同样依赖云 API 的候选列为首装。

### 根因

「仓库自述」是被索引、被缓存、无人维护的**营销文本**，不是事实源。
调研任务的正确形态是：**结论只允许来自逐文件实测 + 落盘证据**，
引用外部元数据必须标注抓取时间并接受复核。

### 固化的规矩

1. 候选/调研类任务的验收判据加入「**目录树实测计数**，与自述冲突时以实测为准、
   数字标注抓取日期」；
2. 「按子件挑装、勿全量」成为外部仓库默认策略——全量安装把别人的工作流偏好
   一起搬进自己家；
3. **能力等价性要核到实现层**：同名功能 ≠ 同链路（引擎、数据格式、是否联网都要对）；
4. 发现上游报告失真后，产出**更正清单并逐条回写到所有下游产物**（卡片、任务书、
   面板），不允许「报告里改了、卡片上没改」；
5. 口径矛盾（同一判据两种用法）本身按缺陷上报，交人定口径后同类候选**同判**。

## 案例二：「绿灯 ≠ 健康」——自检全过而地基是烂的

### 事故

框架自检脚本上线后一直显示「3 项检查全部 PASS」，团队据此认为 skill 体系健康。
直到一次全量质量盘点（五信号逐件实测）撕开盖子：

- 路由表声称覆盖全部 skill，**实测核心 66 个、表内只引用 54 个**，12 个漏收；
  表里「skill 名与核心目录实际一致」这句话自述为真、实为假；
- 多处物理副本与单一真源内容不一致（副本漂移），抽查方向全部是「副本落后核心」；
- 数据库注册名与目录名错配（一件以别名注册）——所有按名统计的自检、台账、
  路由查询对它全线误判「未使用/缺失」。

### 根因

自检脚本的**检查面太窄**：只查了「文件在不在、格式对不对」，
没查「账实是否一致、副本是否同步、名实是否对应」。
绿灯的含义永远是「被检查的那几项通过」，不是「体系健康」——
**绿灯的语义上限 = 你查了什么**。

### 固化的规矩

1. 自检脚本从 3 项扩到 7 项：路由表每行可解析**且引用存在**、路由表引用数 vs
   核心目录数对账、两端链接指向一致、总则管理块配对、任务包 `skills` 字段必填、
   敏感信息扫描等；
2. **每次事故都要问**：现有自检能不能拦住它？不能 → 自检面加一项，并在台账留痕；
3. 镜像教训同样成立：**「46 套测试全绿」≠「卡死缺陷已修复」**——
   回归测试必须对着具体缺陷写，总绿是聚合值，不是逐项证明（[05 §5](05-工程纪律.md)）；
4. 盘点/体检类任务本身只读 + 全量证据，且「先定分层规则再套数据」，
   保证第三方可复核——规则后定，结论必然是先射箭后画靶。

## 案例三：并行派发的文件冲突预防

### 事故（未遂）形态

多执行 Agent 并行跑不同任务时，最先爆的不是逻辑错，是**文件层互踩**：
两个任务改同一份台账/状态文件；安装任务顺手「整理」了另一个任务正在改的目录；
后写的把先写的覆盖掉且无人知晓。文件系统没有「合并友好」的天性，
并发写 = 竞态，只是多数时候没人并发所以没炸。

### 固化的规矩

1. **派发包必写并行声明**：授权栏明确「与 TASK-Y 并行（文件零重叠）」——
   零重叠是批准并行的前提，不是愿望；
2. **禁止项必含「不碰并行任务文件」**：每个任务的红线清单里显式排除他人的落盘区；
3. 步骤表的并行标记 `[P]` 与依赖 `(depends on T0xx)` **内联写死**，
   出稿自检要求两者与禁止项中的并行声明一致——三处不一致即门禁不过；
4. 写权限收敛：**状态文件单一写入者**（协调端），执行端只写自己 RUN 目录——
   从根上消灭最高危的共享写点（[02 §5](02-设计与执行分工.md)）；
5. 安装类任务强制**冲突预检**：新条目名 vs 全部现存命名空间逐一比对，
   同名即 blocked 上报，**不得自动加前缀了事**（重名意味着两个真源）；
6. 发现冲突时按契约返工：受影响任务全部标记冲突文件、按各自交接包重做受影响步骤
   ——没有「最后写入者胜」。

### 一条边界

并行只适用于**文件零重叠**的任务；需要接力（B 读 A 的产物）时改成
显式依赖串链，别用「大概错开时间」冒充依赖管理。

## 案例四：「先装后问」的流程坑与门禁固化

### 事故

skill 补货高峰期存在一段「看到合适的就装」的时期：从候选面板直接圈、
直接从 GitHub 拉默认分支装上。事后想回答三个问题都答不上来——
装的到底是**哪个版本**（默认分支一直在动）？**来源可不可信**（license 没看过，
有个仓库整树零授权声明）？**装进来什么**（aux 引用文件没核，两件还引用了
根本不存在的命令，dangling 至今）。

### 固化的规矩：装 skill 门禁（五道闸，任一不过即不装）

```
0 前提     查重 + 缺口确认；「拟装+用途+数据等级」写入任务文件作为契约
1 验来源   仓库 URL / 维护者 / license 白名单（传染型许可需单独评估；无 license 不合格）
2 验版本   pin 到 commit hash 或签名 tag；禁止「拉最新直接装」
3 验完整性 本地 SHA256 与发布校验值比对；git 仓以 pin + 逐件哈希为等价锚点
4 人工闸门 来源/版本/完整性 + 安装方案（目录/依赖/读写范围/回滚）→ 人批准后才动手
5 装后自检 入单一真源 → 重建两端链接 → 框架自检脚本全绿才可交付
```

要点：

- **安装 skill = 修改协作框架**，按契约级（最高风险档）对待，不是普通文件复制；
- 该门禁很快展示了价值：一个生态官方仓因**整树无授权声明**被当场 blocked、
  附证据上报，人拍板例外后才装——此时「凭什么装」已有完整档案（pin 值 +
  逐件哈希 + 裁决依据），三个月后仍可审计；
- 装后动作同样固化：逐字节不动源文件、frontmatter 名与目录名逐件核对
  （防案例二的「名实错配」复发）、路由表逐件加行并留改前后对照。

## 案例五：文件/目录迁移四条规矩（协调端复盘并入）

### 事故形态

治理资产（总则文件、单一真源库）的搬迁是「看起来最简单、实际最容易翻车」的操作：
目录链接不会跟着物理目录走，手拷两半途中断掉就是两个半截真源，
「应该已经搬完了」的状态记录在半小时后被证明是幻觉。

### 固化的规矩

1. **目录链接（junction/符号链接）不可随父目录移动**：搬迁前必须先解除旧链接，
   移动完成后按台账**重建并验证解析**——链接的本质是路径，路径变了它就成了悬空指针；
2. **目录移动用系统自带的原子搬运工具**（如 robocopy /MOVE 一类），
   避免手拷+手删的两段式操作——中途失败时留下「两边都有」的脏状态，恢复成本翻倍；
3. **写「已迁移」状态前，必须在目标处当场实测**：存在性验证 + 原位置不存在验证，
   两边都核了才许改状态；状态更新永远基于当场证据，不基于「命令跑完了」的推断
   （这正是铁律 4「证据优于自述」对文件系统操作的直接应用）；
4. **位置迁移与内容修订不分在同一批**：一次提交只做搬家或只做改内容——
   混在一起后，diff 无法审计「动的是地址还是实质」，回滚也会把两类变更一起吞掉
   （铁律 3「范围锁定」+ 铁律 5「可回滚」在文件层的推论）。

这四条的共同底色：**文件系统状态是最容易「以为」而最难「验证」的东西**，
所有搬运规矩都指向同一件事——把「以为搬完了」变成「复验过搬完了」。

## 附注：两个环境层小坑（同样值得进案例集）

- **「命令找不到」≠「没安装」**：一次排查把已安装的 CLI 工具误判为缺失，
  根因是当前进程的环境变量块是启动时的过期快照。规矩：下「不存在」结论前，
  先从系统注册表刷新 PATH 复验一次。失败命令最多原样重试一次，
  结论必须建立在复验后的现实上（[05 §3](05-工程纪律.md)）。
- **文件时间戳会撒谎**：复制/搬迁会刷新 mtime，「时间戳不同」不等于「内容不同」；
  副本比对一律比内容哈希，不比时间。

## 收束：这四条坑的共同结构

| 坑 | 错误信任 | 修正后的信任来源 |
|----|----------|------------------|
| 仓库自述不可信 | 别人写的摘要 | 逐文件实测 + 落盘证据 |
| 绿灯≠健康 | 检查通过的聚合值 | 检查面本身被审计 |
| 并行文件冲突 | 「大概不会撞上」 | 契约声明的文件零重叠 |
| 先装后问 | 「看起来靠谱」 | 来源/版本/完整性三道锚点 + 人批准 |
| 迁移想当然 | 「命令跑完了」 | 目标处当场复验 + 搬家与改内容分批 |

一句话总纲：**这套制度里，一切「信任」都必须兑换成「可复查的证据」**——
这正是「以证据而不是模型自述判定完成」这一核心信条在资产与流程层的推演。

上一篇 ← [05-工程纪律](05-工程纪律.md) ｜ 回到 [README](README.md)
